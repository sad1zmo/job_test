server {
    listen 80;
    server_name _;

    location /images/ {
        autoindex on;
        root /var/www/html;
        expires {{ nginx_cache_expiry }};
        add_header Cache-Control "public";
    }

    location / {
        root /var/www/html;
        index index.nginx-debian.html;
    }
}